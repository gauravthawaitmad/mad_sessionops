# Staging overrides for dev.sessionops.makeadiff.in
# On EC2, set BACKEND_ENV_FILE in the root .env file:
#   echo "BACKEND_ENV_FILE=./mad_sessionops_backend/.env.staging" >> .env
#   echo "NEXT_PUBLIC_GOOGLE_CLIENT_ID=xxx" >> .env
#
# Usage (on EC2):
#   docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d --build

services:

  frontend:
    build:
      args:
        NEXT_PUBLIC_API_URL: https://dev.sessionops.makeadiff.in/api
        NEXT_PUBLIC_APP_URL: https://dev.sessionops.makeadiff.in
        NEXT_PUBLIC_APP_ENV: staging
        NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${NEXT_PUBLIC_GOOGLE_CLIENT_ID}
        NEXT_PUBLIC_ENABLE_ANALYTICS: "false"
        NEXT_PUBLIC_ENABLE_DEBUG: "true"
        NEXT_PUBLIC_ENABLE_MOCK_API: "false"
        NEXT_PUBLIC_LOG_LEVEL: info
        NEXT_PUBLIC_API_TIMEOUT: "30000"
